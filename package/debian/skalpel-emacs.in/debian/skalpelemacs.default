# Defaults for skalpelemacs initscript
# sourced by /etc/init.d/skalpelemacs
# installed at /etc/default/skalpelemacs by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
